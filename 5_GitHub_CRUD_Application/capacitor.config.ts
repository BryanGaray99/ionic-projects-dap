import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: '11-gh-client-post',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
